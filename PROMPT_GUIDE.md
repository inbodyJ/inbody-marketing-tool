# InBody 마케팅 툴 — 사용 가이드

개발자가 아닌 마케터를 위한 상세 사용 설명서입니다.

---

## 시작하기 전에

앱을 사용하려면 두 가지가 필요합니다:

1. **Flask 서버 실행** — 담당자에게 문의하거나 별도 안내에 따라 `python app.py` 실행
2. **브라우저에서 앱 열기** — `index.html`을 브라우저로 열기

왼쪽 사이드바에 **초록 점**이 표시되면 서버가 정상 연결된 것입니다.

---

## 1. 에셋 생성 마법사

> SNS 피드, B2B 브로슈어, 이벤트 배너, YouTube 썸네일 등을 자동으로 만듭니다.

### 사용 방법

**1단계 — 기본 정보 입력**

- **국가**: 🇦🇺 AU (호주) 또는 🇬🇧 UK (영국) 선택
- **카테고리**: SNS / B2B / Event / YouTube / Shorts 중 선택
- **채널**: 에셋을 사용할 플랫폼 (예: Instagram, LinkedIn)
- **마케팅 목표**: 달성하고자 하는 목표를 간략히 작성
  - 예시: "gym owners에게 InBody 970 체성분 분석기를 소개하고 체험 예약을 유도한다"
- **제품**: 홍보할 제품명 (예: InBody 970, InBody BAND)
- **행사/시즌**: 크리스마스, EOFY, 학교 복귀 시즌 등 (선택 사항)

**2단계 — AI 카피 생성**

"카피 생성" 버튼을 누르면 AI가 헤드라인과 서브카피를 자동 작성합니다.
마음에 들지 않으면 **재생성** 버튼으로 다시 만들 수 있습니다.
직접 수정도 가능합니다.

**3단계 — 이미지 생성**

"이미지 프롬프트 생성" 버튼을 누르면 AI가 이미지 설명을 작성합니다.
"이미지 생성" 버튼을 누르면 실제 이미지가 만들어집니다.

> ⚠️ 이미지 생성에는 Gemini API 키가 필요합니다. 키가 없으면 프롬프트만 저장됩니다.

**4단계 — 저장**

- 생성된 에셋은 **에셋 라이브러리**에 자동 저장됩니다
- 이미지 프롬프트는 **프롬프트 라이브러리**에도 함께 저장됩니다

---

## 2. 에셋 라이브러리

> 생성한 모든 에셋을 한눈에 보고 다운로드합니다.

### 필터 사용법

| 필터 | 설명 |
|------|------|
| 국가 | 🇦🇺 AU / 🇬🇧 UK 버튼으로 빠르게 필터 |
| 카테고리 | SNS, B2B, Event 등으로 분류 |
| 기간 | 시작일~종료일 범위로 검색 |
| 키워드 | 헤드라인, 카피 텍스트 검색 |

**초기화** 버튼을 누르면 모든 필터가 해제됩니다.

### 에셋 카드 기능

- **이미지 hover** → 상세보기 버튼 표시
- **🔍 상세보기** → 이미지, 헤드라인, 카피 전체 내용 확인
- **⬇️ 다운로드** → 이미지를 PNG 파일로 저장
- **🗑️ 삭제** → 에셋 영구 삭제 (복구 불가)

---

## 3. 프롬프트 라이브러리

> AI가 만들어낸 이미지 생성 프롬프트를 관리하고 재사용합니다.

### 별점 평가

각 프롬프트에 ★ 1~5개로 평가를 매길 수 있습니다.
좋은 결과물을 낳은 프롬프트에 별점을 주면 나중에 찾기 쉽습니다.

### 프롬프트 복사

📋 **복사** 버튼을 누르면 클립보드에 복사됩니다.
복사 횟수가 **사용 횟수**로 기록됩니다.

### 마법사에서 재사용

라이브러리의 프롬프트를 **에셋 생성 마법사**의 3단계에 바로 적용할 수 있습니다.
프롬프트 카드의 **마법사에서 사용** 버튼을 클릭하세요.

### 직접 추가

"+ 프롬프트 추가" 버튼으로 수동으로 프롬프트를 등록할 수 있습니다.
외부에서 찾은 좋은 프롬프트를 보관하는 데 유용합니다.

### 정렬/필터

| 정렬 기준 | 설명 |
|-----------|------|
| 최신순 | 최근에 저장된 순서 |
| 사용 많은 순 | 복사 횟수가 많은 순서 |
| 별점 높은 순 | 평가 점수가 높은 순서 |

---

## 4. YouTube 처리

> YouTube 영상 URL만 입력하면 분석, 카피, 자막 Shorts 영상을 자동으로 생성합니다.

### 처리 단계

```
1. 추출   — YouTube 자막(자동 생성 포함) 다운로드
2. 분석   — AI가 영상 핵심 메시지·타임스탬프 분석
3. 카피   — 분석 결과 기반으로 마케팅 카피 자동 작성
4. 이미지 — 썸네일 이미지 자동 생성
5. Shorts — 9:16 세로 영상으로 자동 편집 (ffmpeg 필요)
6. 완료   — 결과 다운로드 가능
```

### 사용 방법

1. YouTube URL 입력란에 영상 주소를 붙여넣기
2. 제목 입력 (선택 사항 — 비워두면 영상 제목 사용)
3. **처리 시작** 버튼 클릭
4. 단계별 진행 상황이 실시간으로 표시됩니다
5. 완료 후 카피, 이미지, 영상을 확인 및 다운로드

> ℹ️ Shorts 영상 생성에는 서버에 ffmpeg가 설치되어 있어야 합니다.
> 자막이 없는 영상은 처리가 제한될 수 있습니다.

### 처리 히스토리

페이지 하단 **처리 히스토리** 테이블에서 이전 작업 결과를 다시 확인할 수 있습니다.

---

## 5. 대시보드

> 전체 활동 현황을 한눈에 파악합니다.

### KPI 카드

| 카드 | 내용 |
|------|------|
| 총 에셋 | 생성된 전체 에셋 수 |
| 총 프롬프트 | 라이브러리에 저장된 프롬프트 수 |
| 처리된 YouTube | 완료된 YouTube 작업 수 |
| 국가 비율 | AU/UK 에셋 비율 (%) |

### 차트

- **카테고리별 에셋** (막대 차트) — SNS/B2B/Event/YouTube/Shorts 분포
- **국가별 비율** (도넛 차트) — AU vs UK
- **14일 추이** (선 차트) — 최근 2주간 에셋 생성 추이

### 데이터 관리

| 기능 | 설명 |
|------|------|
| 전체 내보내기 (JSON) | 모든 에셋·프롬프트·YouTube 이력을 JSON 파일로 백업 |
| 가져오기 | 백업한 JSON 파일을 불러와 데이터 복원 |
| 전체 초기화 | 모든 데이터를 삭제 (되돌리기 불가, 2회 확인) |

---

## 자주 묻는 질문

**Q. 이미지가 생성되지 않아요.**
A. Gemini API 키가 필요합니다. 담당자에게 문의하세요.
키 없이도 카피(헤드라인, 서브카피)와 프롬프트는 정상 생성됩니다.

**Q. 생성된 내용이 마음에 들지 않아요.**
A. 마법사 2단계에서 **재생성** 버튼을 누르면 다시 만들어줍니다.
텍스트를 직접 수정한 뒤 저장하는 것도 가능합니다.

**Q. 에셋을 실수로 삭제했어요.**
A. localStorage에서 삭제되므로 복구가 불가합니다.
중요한 에셋은 대시보드에서 **전체 내보내기**로 백업해두세요.

**Q. 앱을 닫아도 데이터가 남아 있나요?**
A. 네, 같은 브라우저에서는 localStorage에 데이터가 유지됩니다.
다른 기기나 다른 브라우저에서는 보이지 않습니다.

**Q. 팀원과 데이터를 공유하고 싶어요.**
A. 대시보드 → **전체 내보내기**로 JSON 파일을 다운로드한 후,
팀원이 **가져오기**로 불러오면 됩니다.

**Q. 국가나 카테고리를 새로 추가하고 싶어요.**
A. README.md의 "드롭다운 항목 추가" 섹션을 참고하거나, 개발자에게 문의하세요.

---

## 이미지 프롬프트 작성 팁

AI가 좋은 이미지를 생성하려면 구체적인 프롬프트가 필요합니다.
자동 생성된 프롬프트가 마음에 들지 않을 때는 아래 요소를 추가해보세요:

```
[장면 묘사] + [스타일] + [조명] + [색상] + [브랜드 요소]

예시:
"Professional gym setting with InBody 970 body composition analyzer
in the foreground, athletic trainer and client reviewing results
on screen, clean clinical white background, bright studio lighting,
InBody red accent colors, photorealistic, high quality commercial photography"
```

**피해야 할 표현**
- 특정 실존 인물 이름
- 경쟁사 브랜드명
- 저작권이 있는 캐릭터나 디자인

**효과적인 수식어**
- 스타일: `photorealistic`, `professional photography`, `clean minimal`
- 조명: `bright studio lighting`, `natural daylight`, `soft diffused light`
- 분위기: `energetic`, `trustworthy`, `clinical precision`, `wellness lifestyle`
